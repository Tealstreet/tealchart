# Pine v6 Reference Snapshot Integrity V1

Generated at 2026-09-11T18:15:23.626Z. Measured at commit `56777b842a+dirty`.

## Method

Fetched TradingView's live v6 reference page: https://www.tradingview.com/pine-script-reference/v6/
Reference app bundle: `https://static.tradingview.com/static/bundles/55067.6fdf133257f77dbe3cca.js`
Runtime bundle: `https://static.tradingview.com/static/bundles/runtime.161df4617bbf90839bbc.js`
V6 reference module: `742609`; chunks: `41280`, `81395`, `21857`, `32258`, `42609`.

The script evaluates the deployed TradingView reference bundle in a Node VM with translation calls stubbed, then compares live `functions` plus `methods` signature entries against `PINE_V6_REFERENCE_SIGNATURES`. It deliberately does not derive the oracle from TealScript artifacts.

Method boundary: this compares the live reference bundle, not a TradingView compiler trace. A live-documented parameter is strong reference evidence for acceptance; a live-missing parameter is not by itself proof that TradingView rejects it.

## Headline

Compared 372 committed snapshot callable members against 128 additional live-documented callable members absent from the snapshot.

- Exact live parameter-union matches: 342
- Local snapshot members absent from live callable entries: 24
- Live-documented members absent from the snapshot: 128
- Snapshot parameters absent from live entries: 4
- Live parameters absent from snapshot entries: 9
- Overload shape mismatches: 0

Action buckets:

- Live documents but snapshot lacks: 137 rows. These are potential false refusals or missing implementations.
- Snapshot has but live callable entries lack: 28 rows. These are possible over-acceptance risks, but `matrix.sort:sort_field` proved that absence from one source is not rejection evidence.
- Signature shape differs: 0 rows. These are positional-binding risks: the call may be accepted but bound against the wrong arity/order.

The error rate is not near zero. The committed reference snapshot is materially out of date/thinner than TradingView's live v6 reference bundle, and one immediate conclusion reverses the previous `matrix.sort` finding: `sort_field` is live-documented for the UDT matrix overload, so treating it as invented was wrong.

This supersedes the earlier `235` divergence count from the function-only comparison and the intermediate `409` breakdown from the raw method-label comparison. Including live reference methods is required for the callable surface, but method rows must be keyed from their syntax callables such as `array.get` and `matrix.sort`, not display labels such as `get` or `sort`. The current total reported here is the syntax-keyed callable count.

Recommendation: do not silently refresh all denominators from live in place. Refreshing is the right direction, but it should be a named migration report because it will move the `861` official-name denominator, member maps, signature maps, and coverage fractions. Existing value oracles are lower blast radius than the signature/name instruments: the current provenance report says the value vectors cite live pages and hand derivations, not this committed snapshot.

## Immediate Finding

- `matrix.sort:sort_field` is present in the live v6 reference as `matrix.sort(id, column, order, sort_field)` for matrix values whose elements are UDT IDs. It is also described in TradingView's live matrices documentation under sorting matrices of user-defined types. This is not a corrupt snapshot row; the post-audit rejection was the corrupt state.

## High-Signal Divergences

- snapshot-param-extra: `bgcolor:transp` - The committed snapshot names this parameter, but the live v6 reference does not. Some rows are real over-acceptance risks; some are local aliases or shorthand for variadic notation and need per-member judgement before changing behavior.
- snapshot-param-extra: `input.text_area:inline` - The committed snapshot names this parameter, but the live v6 reference does not. Some rows are real over-acceptance risks; some are local aliases or shorthand for variadic notation and need per-member judgement before changing behavior.

## Rows

| Kind | Member | Param | Local Params | Live Params | Live Syntax | Note |
| --- | --- | --- | --- | --- | --- | --- |
| live-member-missing-from-snapshot | `alert` |  |  | `message`, `freq` | `alert(message, freq) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `alertcondition` |  |  | `condition`, `title`, `message` | `alertcondition(condition, title, message) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `bool` |  |  | `x` | `bool(x) → const bool`<br>`bool(x) → input bool`<br>`bool(x) → simple bool`<br>`bool(x) → series bool` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box` |  |  | `x` | `box(x) → series box` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.copy` |  |  | `id` | `box.copy(id) → series box`<br>`box.copy(id) → series box` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.delete` |  |  | `id` | `box.delete(id) → void`<br>`box.delete(id) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.get_bottom` |  |  | `id` | `box.get_bottom(id) → series float`<br>`box.get_bottom(id) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.get_left` |  |  | `id` | `box.get_left(id) → series int`<br>`box.get_left(id) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.get_right` |  |  | `id` | `box.get_right(id) → series int`<br>`box.get_right(id) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.get_top` |  |  | `id` | `box.get_top(id) → series float`<br>`box.get_top(id) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_bgcolor` |  |  | `id`, `color` | `box.set_bgcolor(id, color) → void`<br>`box.set_bgcolor(id, color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_border_color` |  |  | `id`, `color` | `box.set_border_color(id, color) → void`<br>`box.set_border_color(id, color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_border_style` |  |  | `id`, `style` | `box.set_border_style(id, style) → void`<br>`box.set_border_style(id, style) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_border_width` |  |  | `id`, `width` | `box.set_border_width(id, width) → void`<br>`box.set_border_width(id, width) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_bottom` |  |  | `id`, `bottom` | `box.set_bottom(id, bottom) → void`<br>`box.set_bottom(id, bottom) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_bottom_right_point` |  |  | `id`, `point` | `box.set_bottom_right_point(id, point) → void`<br>`box.set_bottom_right_point(id, point) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_extend` |  |  | `id`, `extend` | `box.set_extend(id, extend) → void`<br>`box.set_extend(id, extend) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_left` |  |  | `id`, `left` | `box.set_left(id, left) → void`<br>`box.set_left(id, left) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_lefttop` |  |  | `id`, `left`, `top` | `box.set_lefttop(id, left, top) → void`<br>`box.set_lefttop(id, left, top) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_right` |  |  | `id`, `right` | `box.set_right(id, right) → void`<br>`box.set_right(id, right) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_rightbottom` |  |  | `id`, `right`, `bottom` | `box.set_rightbottom(id, right, bottom) → void`<br>`box.set_rightbottom(id, right, bottom) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text` |  |  | `id`, `text` | `box.set_text(id, text) → void`<br>`box.set_text(id, text) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_color` |  |  | `id`, `text_color` | `box.set_text_color(id, text_color) → void`<br>`box.set_text_color(id, text_color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_font_family` |  |  | `id`, `text_font_family` | `box.set_text_font_family(id, text_font_family) → void`<br>`box.set_text_font_family(id, text_font_family) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_formatting` |  |  | `id`, `text_formatting` | `box.set_text_formatting(id, text_formatting) → void`<br>`box.set_text_formatting(id, text_formatting) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_halign` |  |  | `id`, `text_halign` | `box.set_text_halign(id, text_halign) → void`<br>`box.set_text_halign(id, text_halign) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_size` |  |  | `id`, `text_size` | `box.set_text_size(id, text_size) → void`<br>`box.set_text_size(id, text_size) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_valign` |  |  | `id`, `text_valign` | `box.set_text_valign(id, text_valign) → void`<br>`box.set_text_valign(id, text_valign) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_text_wrap` |  |  | `id`, `text_wrap` | `box.set_text_wrap(id, text_wrap) → void`<br>`box.set_text_wrap(id, text_wrap) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_top` |  |  | `id`, `top` | `box.set_top(id, top) → void`<br>`box.set_top(id, top) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_top_left_point` |  |  | `id`, `point` | `box.set_top_left_point(id, point) → void`<br>`box.set_top_left_point(id, point) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `box.set_xloc` |  |  | `id`, `left`, `right`, `xloc` | `box.set_xloc(id, left, right, xloc) → void`<br>`box.set_xloc(id, left, right, xloc) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `copy` |  |  |  | `chart.point.copy(id) → chart.point` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `dayofmonth` |  |  | `time`, `timezone` | `dayofmonth(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `dayofweek` |  |  | `time`, `timezone` | `dayofweek(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `fixnan` |  |  | `source` | `fixnan(source) → series float`<br>`fixnan(source) → series int`<br>`fixnan(source) → series color` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `float` |  |  | `x` | `float(x) → const float`<br>`float(x) → input float`<br>`float(x) → simple float`<br>`float(x) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `hour` |  |  | `time`, `timezone` | `hour(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `indicator` |  |  | `title`, `shorttitle`, `overlay`, `format`, `precision`, `scale`, `max_bars_back`, `timeframe`, `timeframe_gaps`, `explicit_plot_zorder`, `max_lines_count`, `max_labels_count`, `max_boxes_count`, `calc_bars_count`, `max_polylines_count`, `dynamic_requests`, `behind_chart` | `indicator(title, shorttitle, overlay, format, precision, scale, max_bars_back, timeframe, timeframe_gaps, explicit_plot_zorder, max_lines_count, max_labels_count, max_boxes_count, calc_bars_count, max_polylines_count, dynamic_requests, behind_chart) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `int` |  |  | `x` | `int(x) → simple int`<br>`int(x) → input int`<br>`int(x) → const int`<br>`int(x) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label` |  |  | `x` | `label(x) → series label` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.copy` |  |  | `id` | `label.copy(id) → series label`<br>`label.copy(id) → series label` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.delete` |  |  | `id` | `label.delete(id) → void`<br>`label.delete(id) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.get_text` |  |  | `id` | `label.get_text(id) → series string`<br>`label.get_text(id) → series string` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.get_x` |  |  | `id` | `label.get_x(id) → series int`<br>`label.get_x(id) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.get_y` |  |  | `id` | `label.get_y(id) → series float`<br>`label.get_y(id) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_color` |  |  | `id`, `color` | `label.set_color(id, color) → void`<br>`label.set_color(id, color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_point` |  |  | `id`, `point` | `label.set_point(id, point) → void`<br>`label.set_point(id, point) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_size` |  |  | `id`, `size` | `label.set_size(id, size) → void`<br>`label.set_size(id, size) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_style` |  |  | `id`, `style` | `label.set_style(id, style) → void`<br>`label.set_style(id, style) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_text` |  |  | `id`, `text` | `label.set_text(id, text) → void`<br>`label.set_text(id, text) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_text_font_family` |  |  | `id`, `text_font_family` | `label.set_text_font_family(id, text_font_family) → void`<br>`label.set_text_font_family(id, text_font_family) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_text_formatting` |  |  | `id`, `text_formatting` | `label.set_text_formatting(id, text_formatting) → void`<br>`label.set_text_formatting(id, text_formatting) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_textalign` |  |  | `id`, `textalign` | `label.set_textalign(id, textalign) → void`<br>`label.set_textalign(id, textalign) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_textcolor` |  |  | `id`, `textcolor` | `label.set_textcolor(id, textcolor) → void`<br>`label.set_textcolor(id, textcolor) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_tooltip` |  |  | `id`, `tooltip` | `label.set_tooltip(id, tooltip) → void`<br>`label.set_tooltip(id, tooltip) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_x` |  |  | `id`, `x` | `label.set_x(id, x) → void`<br>`label.set_x(id, x) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_xloc` |  |  | `id`, `x`, `xloc` | `label.set_xloc(id, x, xloc) → void`<br>`label.set_xloc(id, x, xloc) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_xy` |  |  | `id`, `x`, `y` | `label.set_xy(id, x, y) → void`<br>`label.set_xy(id, x, y) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_y` |  |  | `id`, `y` | `label.set_y(id, y) → void`<br>`label.set_y(id, y) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `label.set_yloc` |  |  | `id`, `yloc` | `label.set_yloc(id, yloc) → void`<br>`label.set_yloc(id, yloc) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `library` |  |  | `title`, `overlay`, `dynamic_requests` | `library(title, overlay, dynamic_requests) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line` |  |  | `x` | `line(x) → series line` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.copy` |  |  | `id` | `line.copy(id) → series line`<br>`line.copy(id) → series line` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.delete` |  |  | `id` | `line.delete(id) → void`<br>`line.delete(id) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.get_price` |  |  | `id`, `x` | `line.get_price(id, x) → series float`<br>`line.get_price(id, x) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.get_x1` |  |  | `id` | `line.get_x1(id) → series int`<br>`line.get_x1(id) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.get_x2` |  |  | `id` | `line.get_x2(id) → series int`<br>`line.get_x2(id) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.get_y1` |  |  | `id` | `line.get_y1(id) → series float`<br>`line.get_y1(id) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.get_y2` |  |  | `id` | `line.get_y2(id) → series float`<br>`line.get_y2(id) → series float` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_color` |  |  | `id`, `color` | `line.set_color(id, color) → void`<br>`line.set_color(id, color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_extend` |  |  | `id`, `extend` | `line.set_extend(id, extend) → void`<br>`line.set_extend(id, extend) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_first_point` |  |  | `id`, `point` | `line.set_first_point(id, point) → void`<br>`line.set_first_point(id, point) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_second_point` |  |  | `id`, `point` | `line.set_second_point(id, point) → void`<br>`line.set_second_point(id, point) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_style` |  |  | `id`, `style` | `line.set_style(id, style) → void`<br>`line.set_style(id, style) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_width` |  |  | `id`, `width` | `line.set_width(id, width) → void`<br>`line.set_width(id, width) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_x1` |  |  | `id`, `x` | `line.set_x1(id, x) → void`<br>`line.set_x1(id, x) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_x2` |  |  | `id`, `x` | `line.set_x2(id, x) → void`<br>`line.set_x2(id, x) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_xloc` |  |  | `id`, `x1`, `x2`, `xloc` | `line.set_xloc(id, x1, x2, xloc) → void`<br>`line.set_xloc(id, x1, x2, xloc) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_xy1` |  |  | `id`, `x`, `y` | `line.set_xy1(id, x, y) → void`<br>`line.set_xy1(id, x, y) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_xy2` |  |  | `id`, `x`, `y` | `line.set_xy2(id, x, y) → void`<br>`line.set_xy2(id, x, y) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_y1` |  |  | `id`, `y` | `line.set_y1(id, y) → void`<br>`line.set_y1(id, y) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `line.set_y2` |  |  | `id`, `y` | `line.set_y2(id, y) → void`<br>`line.set_y2(id, y) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `linefill` |  |  | `x` | `linefill(x) → series linefill` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `linefill.delete` |  |  | `id` | `linefill.delete(id) → void`<br>`linefill.delete(id) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `linefill.get_line1` |  |  | `id` | `linefill.get_line1(id) → series line`<br>`linefill.get_line1(id) → series line` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `linefill.get_line2` |  |  | `id` | `linefill.get_line2(id) → series line`<br>`linefill.get_line2(id) → series line` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `linefill.set_color` |  |  | `id`, `color` | `linefill.set_color(id, color) → void`<br>`linefill.set_color(id, color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `log.error` |  |  | `message`, `formatString`, `arg0, arg1, ...` | `log.error(message) → void`<br>`log.error(formatString, arg0, arg1, ...) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `log.info` |  |  | `message`, `formatString`, `arg0, arg1, ...` | `log.info(message) → void`<br>`log.info(formatString, arg0, arg1, ...) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `log.warning` |  |  | `message`, `formatString`, `arg0, arg1, ...` | `log.warning(message) → void`<br>`log.warning(formatString, arg0, arg1, ...) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `max_bars_back` |  |  | `var`, `num` | `max_bars_back(var, num) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `minute` |  |  | `time`, `timezone` | `minute(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `month` |  |  | `time`, `timezone` | `month(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `na` |  |  | `x` | `na(x) → simple bool`<br>`na(x) → series bool` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `nz` |  |  | `source`, `replacement` | `nz(source, replacement) → simple int`<br>`nz(source, replacement) → simple float`<br>`nz(source, replacement) → simple color`<br>`nz(source, replacement) → series int`<br>`nz(source, replacement) → series float`<br>`nz(source, replacement) → series color` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `polyline.delete` |  |  | `id` | `polyline.delete(id) → void`<br>`polyline.delete(id) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `runtime.error` |  |  | `message` | `runtime.error(message) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `second` |  |  | `time`, `timezone` | `second(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `string` |  |  | `x` | `string(x) → const string`<br>`string(x) → input string`<br>`string(x) → simple string`<br>`string(x) → series string` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `syminfo.prefix` |  |  | `symbol` | `syminfo.prefix(symbol) → simple string`<br>`syminfo.prefix(symbol) → series string` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `syminfo.ticker` |  |  | `symbol` | `syminfo.ticker(symbol) → simple string`<br>`syminfo.ticker(symbol) → series string` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table` |  |  | `x` | `table(x) → series table` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_bgcolor` |  |  | `table_id`, `column`, `row`, `bgcolor` | `table.cell_set_bgcolor(table_id, column, row, bgcolor) → void`<br>`table.cell_set_bgcolor(table_id, column, row, bgcolor) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_height` |  |  | `table_id`, `column`, `row`, `height` | `table.cell_set_height(table_id, column, row, height) → void`<br>`table.cell_set_height(table_id, column, row, height) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text` |  |  | `table_id`, `column`, `row`, `text` | `table.cell_set_text(table_id, column, row, text) → void`<br>`table.cell_set_text(table_id, column, row, text) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text_color` |  |  | `table_id`, `column`, `row`, `text_color` | `table.cell_set_text_color(table_id, column, row, text_color) → void`<br>`table.cell_set_text_color(table_id, column, row, text_color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text_font_family` |  |  | `table_id`, `column`, `row`, `text_font_family` | `table.cell_set_text_font_family(table_id, column, row, text_font_family) → void`<br>`table.cell_set_text_font_family(table_id, column, row, text_font_family) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text_formatting` |  |  | `table_id`, `column`, `row`, `text_formatting` | `table.cell_set_text_formatting(table_id, column, row, text_formatting) → void`<br>`table.cell_set_text_formatting(table_id, column, row, text_formatting) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text_halign` |  |  | `table_id`, `column`, `row`, `text_halign` | `table.cell_set_text_halign(table_id, column, row, text_halign) → void`<br>`table.cell_set_text_halign(table_id, column, row, text_halign) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text_size` |  |  | `table_id`, `column`, `row`, `text_size` | `table.cell_set_text_size(table_id, column, row, text_size) → void`<br>`table.cell_set_text_size(table_id, column, row, text_size) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_text_valign` |  |  | `table_id`, `column`, `row`, `text_valign` | `table.cell_set_text_valign(table_id, column, row, text_valign) → void`<br>`table.cell_set_text_valign(table_id, column, row, text_valign) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_tooltip` |  |  | `table_id`, `column`, `row`, `tooltip` | `table.cell_set_tooltip(table_id, column, row, tooltip) → void`<br>`table.cell_set_tooltip(table_id, column, row, tooltip) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.cell_set_width` |  |  | `table_id`, `column`, `row`, `width` | `table.cell_set_width(table_id, column, row, width) → void`<br>`table.cell_set_width(table_id, column, row, width) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.clear` |  |  | `table_id`, `start_column`, `start_row`, `end_column`, `end_row` | `table.clear(table_id, start_column, start_row, end_column, end_row) → void`<br>`table.clear(table_id, start_column, start_row, end_column, end_row) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.delete` |  |  | `table_id` | `table.delete(table_id) → void`<br>`table.delete(table_id) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.merge_cells` |  |  | `table_id`, `start_column`, `start_row`, `end_column`, `end_row` | `table.merge_cells(table_id, start_column, start_row, end_column, end_row) → void`<br>`table.merge_cells(table_id, start_column, start_row, end_column, end_row) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.set_bgcolor` |  |  | `table_id`, `bgcolor` | `table.set_bgcolor(table_id, bgcolor) → void`<br>`table.set_bgcolor(table_id, bgcolor) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.set_border_color` |  |  | `table_id`, `border_color` | `table.set_border_color(table_id, border_color) → void`<br>`table.set_border_color(table_id, border_color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.set_border_width` |  |  | `table_id`, `border_width` | `table.set_border_width(table_id, border_width) → void`<br>`table.set_border_width(table_id, border_width) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.set_frame_color` |  |  | `table_id`, `frame_color` | `table.set_frame_color(table_id, frame_color) → void`<br>`table.set_frame_color(table_id, frame_color) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.set_frame_width` |  |  | `table_id`, `frame_width` | `table.set_frame_width(table_id, frame_width) → void`<br>`table.set_frame_width(table_id, frame_width) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `table.set_position` |  |  | `table_id`, `position` | `table.set_position(table_id, position) → void`<br>`table.set_position(table_id, position) → void` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `time` |  |  | `timeframe`, `session`, `bars_back`, `timeframe_bars_back`, `timezone` | `time(timeframe, session, bars_back, timeframe_bars_back) → series int`<br>`time(timeframe, session, timezone, bars_back, timeframe_bars_back) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `time_close` |  |  | `timeframe`, `session`, `bars_back`, `timeframe_bars_back`, `timezone` | `time_close(timeframe, session, bars_back, timeframe_bars_back) → series int`<br>`time_close(timeframe, session, timezone, bars_back, timeframe_bars_back) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `timestamp` |  |  | `dateString`, `year`, `month`, `day`, `hour`, `minute`, `second`, `timezone` | `timestamp(dateString) → const int`<br>`timestamp(dateString) → series int`<br>`timestamp(year, month, day, hour, minute, second) → simple int`<br>`timestamp(year, month, day, hour, minute, second) → series int`<br>`timestamp(timezone, year, month, day, hour, minute, second) → simple int`<br>`timestamp(timezone, year, month, day, hour, minute, second) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `weekofyear` |  |  | `time`, `timezone` | `weekofyear(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| live-member-missing-from-snapshot | `year` |  |  | `time`, `timezone` | `year(time, timezone) → series int` | The live v6 reference documents this callable, but the committed signature snapshot has no row for it. |
| local-member-not-in-live-functions | `array.new_chart_point` |  | `size`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `array.new_polyline` |  | `size`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `math.clamp` |  | `val`, `min`, `max` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `math.tanh` |  | `number` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `math.trunc` |  | `number` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.add_column` |  | `id`, `column`, `array_id` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.column` |  | `id`, `column` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.is_valid` |  | `id` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.new_bool` |  | `rows`, `columns`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.new_color` |  | `rows`, `columns`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.new_float` |  | `rows`, `columns`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.new_int` |  | `rows`, `columns`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.new_string` |  | `rows`, `columns`, `initial_value` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `matrix.remove_column` |  | `id`, `column` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.adx` |  | `diLength`, `adxSmoothing` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.bar_index` |  | `source` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.covariance` |  | `source1`, `source2`, `length` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.dema` |  | `source`, `length` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.kst` |  | `source`, `roclength1`, `roclength2`, `roclength3`, `roclength4`, `smalen1`, `smalen2`, `smalen3`, `smalen4`, `signalLength` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.obv` |  | `source`, `volume` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.smma` |  | `source`, `length` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.sum` |  | `source`, `length` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `ta.tema` |  | `source`, `length` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| local-member-not-in-live-functions | `timeframe.to_seconds` |  | `timeframe` |  |  | The committed signature snapshot has this callable, but the live v6 reference does not expose a matching function or method entry. This may be a local extension, legacy alias, generated helper, or over-acceptance risk; it is not safe to classify from manual silence alone. |
| snapshot-param-extra | `bgcolor` | `transp` | `color`, `offset`, `editable`, `show_last`, `title`, `display`, `force_overlay`, `transp` | `color`, `offset`, `editable`, `show_last`, `title`, `display`, `force_overlay` | `bgcolor(color, offset, editable, show_last, title, display, force_overlay) → void` | The committed snapshot names this parameter, but the live v6 reference does not. Some rows are real over-acceptance risks; some are local aliases or shorthand for variadic notation and need per-member judgement before changing behavior. |
| snapshot-param-extra | `input.text_area` | `inline` | `defval`, `title`, `tooltip`, `inline`, `group`, `confirm`, `display`, `active` | `defval`, `title`, `tooltip`, `group`, `confirm`, `display`, `active` | `input.text_area(defval, title, tooltip, group, confirm, display, active) → input string` | The committed snapshot names this parameter, but the live v6 reference does not. Some rows are real over-acceptance risks; some are local aliases or shorthand for variadic notation and need per-member judgement before changing behavior. |
| snapshot-param-extra | `str.length` | `source` | `source` | `string` | `str.length(string) → const int`<br>`str.length(string) → simple int`<br>`str.length(string) → series int` | The committed snapshot names this parameter, but the live v6 reference does not. Some rows are real over-acceptance risks; some are local aliases or shorthand for variadic notation and need per-member judgement before changing behavior. |
| snapshot-param-extra | `str.split` | `source` | `source`, `separator` | `string`, `separator` | `str.split(string, separator) → array<string>` | The committed snapshot names this parameter, but the live v6 reference does not. Some rows are real over-acceptance risks; some are local aliases or shorthand for variadic notation and need per-member judgement before changing behavior. |
| snapshot-param-missing | `fill` | `bottom_color` | `plot1`, `plot2`, `color`, `title`, `editable`, `show_last`, `fillgaps`, `display` | `plot1`, `plot2`, `top_value`, `bottom_value`, `top_color`, `bottom_color`, `title`, `display`, `fillgaps`, `editable`, `hline1`, `hline2`, `color`, `show_last` | `fill(plot1, plot2, top_value, bottom_value, top_color, bottom_color, title, display, fillgaps, editable) → void`<br>`fill(hline1, hline2, color, title, editable, fillgaps, display) → void`<br>`fill(plot1, plot2, color, title, editable, show_last, fillgaps, display) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `fill` | `bottom_value` | `plot1`, `plot2`, `color`, `title`, `editable`, `show_last`, `fillgaps`, `display` | `plot1`, `plot2`, `top_value`, `bottom_value`, `top_color`, `bottom_color`, `title`, `display`, `fillgaps`, `editable`, `hline1`, `hline2`, `color`, `show_last` | `fill(plot1, plot2, top_value, bottom_value, top_color, bottom_color, title, display, fillgaps, editable) → void`<br>`fill(hline1, hline2, color, title, editable, fillgaps, display) → void`<br>`fill(plot1, plot2, color, title, editable, show_last, fillgaps, display) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `fill` | `hline1` | `plot1`, `plot2`, `color`, `title`, `editable`, `show_last`, `fillgaps`, `display` | `plot1`, `plot2`, `top_value`, `bottom_value`, `top_color`, `bottom_color`, `title`, `display`, `fillgaps`, `editable`, `hline1`, `hline2`, `color`, `show_last` | `fill(plot1, plot2, top_value, bottom_value, top_color, bottom_color, title, display, fillgaps, editable) → void`<br>`fill(hline1, hline2, color, title, editable, fillgaps, display) → void`<br>`fill(plot1, plot2, color, title, editable, show_last, fillgaps, display) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `fill` | `hline2` | `plot1`, `plot2`, `color`, `title`, `editable`, `show_last`, `fillgaps`, `display` | `plot1`, `plot2`, `top_value`, `bottom_value`, `top_color`, `bottom_color`, `title`, `display`, `fillgaps`, `editable`, `hline1`, `hline2`, `color`, `show_last` | `fill(plot1, plot2, top_value, bottom_value, top_color, bottom_color, title, display, fillgaps, editable) → void`<br>`fill(hline1, hline2, color, title, editable, fillgaps, display) → void`<br>`fill(plot1, plot2, color, title, editable, show_last, fillgaps, display) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `fill` | `top_color` | `plot1`, `plot2`, `color`, `title`, `editable`, `show_last`, `fillgaps`, `display` | `plot1`, `plot2`, `top_value`, `bottom_value`, `top_color`, `bottom_color`, `title`, `display`, `fillgaps`, `editable`, `hline1`, `hline2`, `color`, `show_last` | `fill(plot1, plot2, top_value, bottom_value, top_color, bottom_color, title, display, fillgaps, editable) → void`<br>`fill(hline1, hline2, color, title, editable, fillgaps, display) → void`<br>`fill(plot1, plot2, color, title, editable, show_last, fillgaps, display) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `fill` | `top_value` | `plot1`, `plot2`, `color`, `title`, `editable`, `show_last`, `fillgaps`, `display` | `plot1`, `plot2`, `top_value`, `bottom_value`, `top_color`, `bottom_color`, `title`, `display`, `fillgaps`, `editable`, `hline1`, `hline2`, `color`, `show_last` | `fill(plot1, plot2, top_value, bottom_value, top_color, bottom_color, title, display, fillgaps, editable) → void`<br>`fill(hline1, hline2, color, title, editable, fillgaps, display) → void`<br>`fill(plot1, plot2, color, title, editable, show_last, fillgaps, display) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `str.length` | `string` | `source` | `string` | `str.length(string) → const int`<br>`str.length(string) → simple int`<br>`str.length(string) → series int` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `str.split` | `string` | `source`, `separator` | `string`, `separator` | `str.split(string, separator) → array<string>` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
| snapshot-param-missing | `strategy` | `calc_bars_count` | `title`, `shorttitle`, `overlay`, `format`, `precision`, `scale`, `pyramiding`, `calc_on_order_fills`, `calc_on_every_tick`, `calc_on_every_history_tick`, `max_bars_back`, `backtest_fill_limits_assumption`, `default_qty_type`, `default_qty_value`, `initial_capital`, `currency`, `slippage`, `commission_type`, `commission_value`, `process_orders_on_close`, `close_entries_rule`, `margin_long`, `margin_short`, `explicit_plot_zorder`, `max_lines_count`, `max_labels_count`, `max_boxes_count`, `risk_free_rate`, `use_bar_magnifier`, `fill_orders_on_standard_ohlc`, `max_polylines_count`, `dynamic_requests`, `behind_chart` | `title`, `shorttitle`, `overlay`, `format`, `precision`, `scale`, `pyramiding`, `calc_on_order_fills`, `calc_on_every_tick`, `max_bars_back`, `backtest_fill_limits_assumption`, `default_qty_type`, `default_qty_value`, `initial_capital`, `currency`, `slippage`, `commission_type`, `commission_value`, `process_orders_on_close`, `close_entries_rule`, `margin_long`, `margin_short`, `explicit_plot_zorder`, `max_lines_count`, `max_labels_count`, `max_boxes_count`, `calc_bars_count`, `risk_free_rate`, `use_bar_magnifier`, `fill_orders_on_standard_ohlc`, `max_polylines_count`, `dynamic_requests`, `behind_chart`, `calc_on_every_history_tick` | `strategy(title, shorttitle, overlay, format, precision, scale, pyramiding, calc_on_order_fills, calc_on_every_tick, max_bars_back, backtest_fill_limits_assumption, default_qty_type, default_qty_value, initial_capital, currency, slippage, commission_type, commission_value, process_orders_on_close, close_entries_rule, margin_long, margin_short, explicit_plot_zorder, max_lines_count, max_labels_count, max_boxes_count, calc_bars_count, risk_free_rate, use_bar_magnifier, fill_orders_on_standard_ohlc, max_polylines_count, dynamic_requests, behind_chart, calc_on_every_history_tick) → void` | The live v6 reference names this parameter, but the committed snapshot does not. If the checker uses the committed snapshot for named-argument binding, this is a potential false rejection. |
