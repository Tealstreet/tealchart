//@version=6
strategy("RSI Mean Reversion", initial_capital=10000, default_qty_value=1)
rsiVal = ta.rsi(close, 7)
if rsiVal < 30
    strategy.entry("Long", strategy.long)
if rsiVal > 70
    strategy.close("Long")
plot(rsiVal, title="RSI")
