//@version=6
strategy("Limit Entry", initial_capital=10000, process_orders_on_close=true)
if bar_index == 0
    strategy.entry("Long", strategy.long, qty=1, limit=99)
if strategy.position_size > 0 and bar_index > 5
    strategy.close("Long")
plot(strategy.position_size, title="Position")
