//@version=6
strategy("Stop Loss Only", initial_capital=10000)
if bar_index == 0
    strategy.entry("Long", strategy.long, qty=2)
if strategy.position_size > 0
    strategy.exit("SL", "Long", stop=strategy.position_avg_price - 4)
plot(strategy.position_size, title="Position")
