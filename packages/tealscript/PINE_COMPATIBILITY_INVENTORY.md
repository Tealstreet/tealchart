# Pine Compatibility Inventory

This file is the canonical row-level Pine compatibility matrix. Keep it aligned
with tests and implementation as parity work lands. `PINE_COMPATIBILITY.md` is
the narrative user-facing status page; `PINE_PARITY_EPICS.md` is the long-term
roadmap.

## Status Values

| Status | Meaning |
| --- | --- |
| `Supported` | Syntax and runtime behavior are implemented and covered by tests. |
| `Partial` | Common usage works, but important Pine behavior, diagnostics, or UI integration is incomplete. |
| `Planned` | The feature is in scope but not implemented yet. |
| `Unsupported` | The feature should produce a clear diagnostic or documented rewrite path. |

## Topic Inventory Row

Use this shape when adding or refreshing compatibility rows:

| Area | Feature | Status | Evidence | Remaining gaps |
| --- | --- | --- | --- | --- |
| Parser, runtime, built-ins, visuals, data, alerts, or strategies | Pine feature or namespace | Status value | Test file, fixture, or diagnostic covering current behavior | Missing Pine behavior or reason unsupported |

Keep evidence specific enough that a reviewer can find the behavior without
reading the whole runtime.

## Current Matrix

Rendering-related rows use `Partial` when TealScript emits and renders useful
Pine-compatible outputs but intentionally stops short of TradingView-exact
geometry, sizing, or pixel behavior. Those fidelity leftovers are tracked
separately from feature gaps that block pasted scripts from parsing, running,
or producing visible drawings.

### Syntax

| Area | Feature | Status | Evidence | Remaining gaps |
| --- | --- | --- | --- | --- |
| Parser | `//@version=6` annotation | Supported | `tests/parser/parser.test.ts`; `tests/compat/pine-basics.test.ts` | No version-specific runtime branches yet. |
| Parser | `//@version=5` annotation | Partial | `tests/parser/parser.test.ts` | Accepted as numeric version only. |
| Parser/runtime | `indicator(...)` | Partial | `tests/compat/pine-basics.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Common declaration options are parsed, and semantic diagnostics cover unknown or strategy-only declaration arguments, visual option expression traversal, declaration format/scale values, non-boolean declaration flags, non-numeric count/precision settings, and literal precision/drawing-limit value diagnostics. Many declaration options are not fully applied. |
| Parser/runtime | `study(...)` legacy declaration | Partial | `tests/parser/parser.test.ts`; `src/parser/parser.test.ts`; `src/semantic/checker.test.ts`; `tests/runtime/runtime.test.ts` | Legacy v3/v4 study declarations parse and execute through the indicator declaration metadata path, including named `resolution` / `resolution_gaps` aliases for `timeframe` / `timeframe_gaps`. Broader old-version Pine built-in/API differences are not modeled. |
| Parser/runtime | `strategy(...)` | Partial | `src/parser/parser.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; Roadmap Epic 14 | Declaration settings, broker-emulator state, orders/fills, trade accessors, source-linked public strategy stats tables, semantic declaration/call-shape diagnostics, and literal declaration value diagnostics for common numeric, enum-like, boolean, format/scale, precision, and drawing-limit settings are covered, including non-numeric shared count/precision settings and `strategy.exit()` target/trailing-offset structure diagnostics. Full TradingView intrabar path parity, bar magnifier, and lower-timeframe fill simulation remain deferred until TealScript has an explicit intrabar data model. |
| Parser/runtime | `library(...)` / `import` | Partial | `tests/parser/parser.test.ts`; `src/parser/parser.test.ts`; `tests/compat/pine-language.test.ts`; `tests/compat/pine-unsupported.test.ts`; `tests/compat/pine-builtins.test.ts`; Public API guardrails in `tests/public-api/public-entry.test.ts`; `src/semantic/checker.test.ts`; `tests/worker/protocol.test.ts`; `tests/worker/TealScriptWorker.test.ts`; `packages/tealchart/src/tealscript/TealscriptManager.test.ts` | `library(...)` declaration metadata including positional `overlay` and `dynamic_requests`, non-boolean declaration flag diagnostics, unknown declaration argument diagnostics, exported local helpers, typed/qualified exported parameters, import syntax and versioned path metadata, host-provided registry dispatch for `alias.exportedFunction(...)`, worker semantic/runtime registry propagation, Tealchart manager registry propagation, exported constants with semantic type inference, imported helper simple/series return qualifier normalization, exported imported UDT constructors via `alias.Type.new(...)`, exported imported methods on imported UDT instances, local and exported imported enum member binding plus missing-member and private-or-missing namespace diagnostics, export-shape diagnostics, exported function scope diagnostics, and exported request-scope diagnostics are covered. Published TradingView lookup, broader enum namespace edge cases, and request-expression qualifier diagnostics remain planned. |
| Parser/runtime | Variable declarations | Supported | `tests/parser/parser.test.ts`; `src/parser/parser.test.ts`; `tests/runtime/runtime.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Parser coverage includes continuation-line initializers after declaration and reassignment operators plus UDT field defaults. Annotated local primitive, reference, collection, UDT, enum, and mixed conditional/if/switch initializer mismatches are diagnosed. Broader qualifier-sensitive type checking is planned. |
| Parser/runtime | `var` / `varip` | Partial | `tests/compat/pine-language.test.ts`; `src/runtime/engine.test.ts`; `tests/compat/pine-real-checkpoints.test.ts` | Root and function-local `var` persistence, primitive `varip` realtime persistence, UDT `varip` field rollback, and a source-linked public `varip` array tick-buffer checkpoint are covered. Broader host-driven intrabar scheduling and nested-scope edge cases remain planned. |
| Parser/runtime | Reassignment `:=` | Partial | `tests/parser/parser.test.ts`; `tests/runtime/runtime.test.ts`; `tests/compat/pine-arrays.test.ts`; `tests/compat/pine-objects.test.ts`; `src/semantic/checker.test.ts` | Identifier, array index, and UDT field assignment are supported. Plain identifier, array index, and UDT field value type mismatches are diagnosed when semantic types are known; plain identifier and UDT field qualifier downgrades are also diagnosed. Broader object/drawing member assignment diagnostics remain planned. |
| Parser/runtime | Compound assignment | Partial | `tests/parser/parser.test.ts`; `tests/runtime/runtime.test.ts`; `tests/compat/pine-arrays.test.ts`; `tests/compat/pine-objects.test.ts`; `src/semantic/checker.test.ts` | Identifier, array index, and UDT field compound assignment are supported. Plain identifier, local UDT field, and array element compound assignments diagnose known unsupported operand and result type mismatches. Broader collection/member compound parity remains planned. |
| Parser/runtime | Tuple destructuring | Supported | `tests/parser/parser.test.ts`; `src/parser/parser.test.ts`; `tests/compat/pine-basics.test.ts`; `tests/compat/pine-language.test.ts`; `src/semantic/checker.test.ts` | Literal tuple destructuring, underscore discard placeholders, direct declaration `if`, `switch`, and loop initializer tuples, direct and compatible if/else plus partial-if user-function tuple returns, direct user-function loop and defaulted or partial switch tuple returns, compatible user-method tuple returns, and known tuple-returning TA calls infer positional element types for downstream diagnostics. Direct tuple initializers, including known local user-call tuple returns and mixed-shape user-call control returns, and direct control initializer tuple arms, including arm-local user-call tuple returns, diagnose obvious non-tuple and arity mismatches. Broader mixed-shape branch, loop, and switch tuple-return inference remains planned. |
| Parser/runtime | Wrapped and continued expressions | Partial | `tests/parser/parser.test.ts`; `tests/compat/pine-language.test.ts` | Multiline declarations, calls, arrays, tuple patterns, index access, parenthesized expressions, wrapped collection annotations, wrapped generic constructors, wrapped request expressions with multiline ternary arguments, line continuations after operators, and inline comments on wrapped continuation lines are supported. Leading comparison, logical, multiplicative, `+`, numeric and non-numeric `-`, and ternary operators are covered by parser/runtime fixtures. Broader arbitrary nested layout hardening remains planned. |
| Parser/runtime | `if` / `else if` / `else` | Supported | `tests/parser/parser.test.ts`; `src/parser/parser.test.ts`; `tests/compat/pine-language.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Compatible user-function if/else, variable initializer, partial if return arms, and eighth-level nested UDF branches infer scalar types for downstream diagnostics. Broader arbitrary nested indentation hardening continues under core semantics. |
| Parser/runtime | Ternary `?:` | Supported | `tests/runtime/runtime.test.ts`; `tests/compat/pine-visuals.test.ts`; `src/semantic/checker.test.ts` | Compatible ternary arms infer scalar types for downstream diagnostics. Broader `na` truthiness fixtures are planned. |
| Parser/runtime | `for` loops | Supported | `tests/parser/parser.test.ts`; `src/runtime/engine.test.ts`; `tests/compat/pine-control-time.test.ts`; `tests/compat/pine-language.test.ts`; `src/semantic/checker.test.ts` | Direct numeric and collection for-loop expressions plus user-function for-loop returns infer scalar types for downstream diagnostics. Runtime limits are enforced; broader performance/sandbox tuning remains in the limits epic. |
| Parser/runtime | `while`, `break`, `continue` | Supported | `tests/parser/parser.test.ts`; `src/runtime/engine.test.ts`; `tests/compat/pine-control-time.test.ts`; `tests/compat/pine-language.test.ts`; `src/semantic/checker.test.ts` | Direct while-loop expressions and user-function while-loop returns infer scalar types for downstream diagnostics. Runtime limits are enforced; broader performance/sandbox tuning remains in the limits epic. |
| Parser/runtime | User-defined functions | Partial | `tests/parser/parser.test.ts`; `tests/compat/pine-language.test.ts`; `src/semantic/checker.test.ts`; `src/runtime/engine.test.ts` | Single-line and multiline UDFs, named arguments, default parameters, local invalid call-shape diagnostics including missing required arguments and positional arguments after named arguments, imported exported-function semantic/runtime call-shape and availability diagnostics, branch and loop returns including seventh-level nested `if` branches, direct and imported semantic return inference, call-site qualifier propagation for annotated parameters, source identity for source parameters, simple source-returning local UDF calls, same-source conditional and switch returns, and recursive-call diagnostics are covered. Deeper arbitrary block parsing, broader expression-level source inference, and broader semantic diagnostics for imported libraries need hardening. |
| Parser/runtime | Methods, e.g. `arr.push(x)` and `method scale(float this)` | Partial | `tests/compat/pine-arrays.test.ts`; `tests/compat/pine-objects.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; `src/runtime/arrays.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Built-in collection method sugar and user-defined method declarations dispatch. Local and imported UDT receiver overloads are covered, and reduced public market-structure object idioms cover persistent UDT state updated through methods plus UDT array records read through method-derived scores. Local semantic method overload declarations are accepted, method return inference selects overloads by receiver specificity plus annotated argument signatures, full and partial branch/switch plus loop control-flow method returns infer scalar and tuple types, source identity for source receiver or parameter passthrough methods, local invalid call-shape diagnostics, imported exported-method semantic/runtime call-shape and availability diagnostics, and local plus import-qualified enum receivers are covered. Broader imported-library method diagnostics, especially qualifier-sensitive request-expression and overload edge cases, remain planned. |
| Parser/runtime | `switch` | Partial | `tests/parser/parser.test.ts`; `tests/compat/pine-control-time.test.ts`; `src/semantic/checker.test.ts` | Compatible defaulted and partial switch expression arms infer scalar types for downstream diagnostics. Exhaustive mixed-shape diagnostics remain planned. |
| Parser/runtime | User-defined types | Partial | `tests/parser/parser.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts`; `tests/compat/pine-objects.test.ts`; `tests/compat/pine-language.test.ts`; `tests/compat/pine-real-checkpoints.test.ts` | Type declarations, fields, constructors, constructor argument diagnostics including local and imported exported constructor argument-order runtime diagnostics, unknown field diagnostics, conservative primitive field type diagnostics, field access/assignment, reference assignment, shallow copy helpers, rollback, user-defined method dispatch, and source-linked public UDT array checkpoint coverage are covered. Full reference/qualified field type enforcement and library diagnostics remain planned. |

### Runtime Semantics

| Area | Feature | Status | Evidence | Remaining gaps |
| --- | --- | --- | --- | --- |
| Runtime | Bar-by-bar execution | Supported | `tests/runtime/runtime.test.ts`; `tests/compat/pine-basics.test.ts` | Realtime rollback semantics remain partial. |
| Runtime | History references `x[n]` | Partial | `tests/compat/pine-language.test.ts`; `src/runtime/engine.test.ts` | Broader type diagnostics and max-bars inference are planned. |
| Runtime | `na` value and `na(value)` | Partial | `tests/compat/pine-language.test.ts`; `src/semantic/checker.test.ts` | Arithmetic propagation, `na()` checks, explicit `bool(na)`, `na` comparison false semantics, direct literal `na` equality/relational comparison diagnostics, literal `na` boolean-context diagnostics, literal `na` bool variable assignment diagnostics, literal `na` bool UDT-field diagnostics, and literal `na` bool user-callable parameter diagnostics are covered. Broader v6 bool type enforcement remains. |
| Runtime | Built-in price series | Supported | `tests/runtime/runtime.test.ts`; `tests/compat/pine-basics.test.ts`; `src/runtime/engine.test.ts` | OHLCV series are covered. Host-provided Pine v6 `bid`/`ask` quote fields are exposed only on `1T`; missing quote fields and non-`1T` contexts return `na`. Broader live broker-feed injection remains host responsibility. |
| Runtime | Calendar variables | Partial | `tests/compat/pine-control-time.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; `src/runtime/engine.test.ts` | Current-bar variables, callable helpers, fixed-offset/IANA timezone arguments, named timezone arguments, exchange-timezone injection, host-provided session closures, and public session-state helper gates are covered for common Pine idioms. Broader exchange calendar catalogs remain host responsibility. |
| Runtime | `bar_index` / `last_bar_index` | Supported | `tests/runtime/runtime.test.ts`; `tests/compat/pine-control-time.test.ts` | None known for current execution model. |
| Runtime | `barstate.*` | Partial | `tests/compat/pine-control-time.test.ts`; `src/runtime/engine.test.ts`; `tests/worker/TealScriptWorker.test.ts` | Historical, appended realtime, replayed realtime, and confirmed realtime barstate transitions are covered through the engine, and the worker wrapper ignores stale realtime tick results. Broader live host scheduling parity remains. |
| Runtime | `syminfo.*` | Partial | `tests/compat/pine-control-time.test.ts`; `tests/compat/pine-request-security.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Common static symbol metadata, host-backed root and ticker ID metadata, main-chart ticker ID preservation in requested contexts, exchange timezone injection, point value, minimum contract, volume type, exchange prefix, session class, classification fields, ISIN/current-contract metadata, employees/shareholders, shares-outstanding fields, futures expiration, analyst recommendation dates, and target-price fields are covered. Semantic analysis preserves known string, int, and float `syminfo.*` member return types for downstream diagnostics. Broader live host symbol metadata injection remains. |
| Runtime | `chart.*` | Partial | `tests/compat/pine-control-time.test.ts`; `src/semantic/checker.test.ts` | Host-injected chart colors, visible bar time metadata, standard chart detection, and non-standard chart-type flags are covered. Renderer theme synchronization remains host responsibility. |
| Runtime | `timeframe.*` | Supported | `tests/compat/pine-control-time.test.ts`; `src/runtime/engine.test.ts` | Current parity covers metadata fields, declaration-time timeframe overrides, ticks/seconds/minutes/D/W/M parsing, `timeframe.in_seconds()`/`timeframe.to_seconds()`, `timeframe.from_seconds()`, `timeframe.change()`, and comparison idioms. Broader live host timeframe injection is outside the runtime contract. |
| Runtime | Function-local series state | Supported | `tests/compat/pine-language.test.ts`; `tests/compat/pine-objects.test.ts`; `src/runtime/engine.test.ts` | Root and function-local `var` persistence, derived series history, direct built-in, simple alias, `input.source()`, and UDF/imported/method parameter source identity for common rolling helper windows, locationless AST call-site fallback, local/imported UDF and method call-site state, and nested function `if`/loop block state are covered. |
| Runtime | `max_bars_back` | Partial | `tests/compat/pine-language.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Explicit indicator-bound enforcement, `max_bars_back(var, num)` function hints, semantic diagnostics for invalid literal declaration and function hint values, static profile inference for literal, simple numeric, selected pure `math.*` including averaging and power/square-root helpers, selected normalization, input-gated conditional offsets, selected static rolling-helper lookbacks including `ta.range()`, `ta.rising()`/`ta.falling()`, statistical helpers, momentum helpers, band/average/regression helpers, oscillator helpers, fixed/default helper lookbacks, selected default-argument helper lookbacks, default-source highest/lowest forms, pivot left/right windows, remaining common TA helpers including static `ta.tr()`, observed `math.sum()` rolling lookbacks, direct OHLC helper lookbacks, TA variables that read prior close/volume bars, and common `ta.*` source-window, retained-source, and recursive helper lookbacks are covered. Broader arbitrary dynamic series-offset inference remains planned. |
| Runtime | Limits, sandboxing, and profiling | Partial | `src/runtime/engine.test.ts`; `src/runtime/drawings/builtins.test.ts`; `tests/parser/parser.test.ts`; `tests/compat/pine-request-security.test.ts`; `tests/worker/protocol.test.ts`; Roadmap Epic 15 | Implemented guardrails cover loop iteration caps, 64 non-`hline()` plot outputs, 40 unique `request.*` contexts per pass, 10,000 live table cells, parser source/depth limits, deterministic `math.random()`, runtime profile counters, and worker profile propagation. Broader hard wall-clock and memory quotas remain planned as host/runtime sandbox hardening. |

### Built-Ins

| Area | Feature | Status | Evidence | Remaining gaps |
| --- | --- | --- | --- | --- |
| Built-ins | Call binding diagnostics | Partial | `src/semantic/checker.test.ts`; `tests/worker/TealScriptWorker.test.ts` | Known built-in calls report unknown named argument, duplicate named argument, duplicate positional/named binding, missing required argument, and invalid positional-after-named diagnostics. Visual-output diagnostics cover `plot()`, `hline()`, `fill()` alias bindings, OHLC `plotbar()` / `plotcandle()` arguments, and marker missing, unknown, and duplicate argument bindings. Drawing constructor diagnostics distinguish coordinate and `chart.point` overload bindings for `line.new()` and `box.new()`, and chart-point constructors diagnose non-numeric coordinate values. Worker wrapper coverage preserves structured semantic diagnostics for editor-facing error callbacks. Full reference-manual overload parity remains. |
| Built-ins | `math.*` | Partial | `tests/compat/pine-builtins.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Runtime covers common constants, variadic numeric helpers, rounding, sliding sum, random, and degree/radian conversions. Semantic analysis preserves known math constant and helper return types for downstream diagnostics, including common int/float overload distinctions, and diagnoses non-numeric math helper arguments. Random behavior and full overload parity remain. |
| Built-ins | `ta.*` | Partial | `tests/compat/pine-basics.test.ts`; `tests/compat/pine-builtins.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | Runtime covers common moving-average, oscillator, statistical, event, cross, pivot, DMI, SAR, linear-regression helpers, and documented TA series variables such as `ta.nvi`, `ta.pvi`, `ta.pvt`, `ta.wad`, and `ta.wvad`. Semantic analysis preserves tuple returns including `ta.kc()` plus known scalar bool/int/float TA helper and variable return types, including scalar `ta.vwap()` overloads plus source-preserving `ta.change()` and `ta.valuewhen()` results, and diagnoses common non-numeric TA helper arguments plus non-boolean TA condition and option arguments. Full reference-manual inventory and edge-case parity remain. |
| Built-ins | `input.*` | Partial | `tests/compat/pine-basics.test.ts`; `tests/compat/pine-builtins.test.ts`; `src/semantic/checker.test.ts` | Runtime covers generic `input()`, common typed helpers, `input.enum()`, and metadata. Semantic analysis preserves generic `input()` scalar defval-derived return types and known `input.*` return types for downstream diagnostics, including `input.source()` defval source types and `input.enum()` defval enum types, and reports known typed default-value diagnostics including `input.color()`, literal default constraint, `options` shape and element-type checks, range/options overload, non-string text metadata, and non-boolean `confirm`/`active` metadata diagnostics. Advanced UI/display behavior and validation remain. |
| Built-ins | Time functions | Partial | `tests/compat/pine-builtins.test.ts`; `tests/compat/pine-control-time.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts` | IANA timezones are covered for common timestamp/calendar/session helpers. Runtime covers known-chart-bar `time()` / `time_close()` `bars_back` offsets, deterministic chart-timeframe future projection for negative `bars_back` offsets beyond known chart bars, and requested-timeframe `timeframe_bars_back` shifts before timeframe/session filtering, including named, no-timezone positional, and timezone positional forms; invalid dynamic time offsets return `na`. Semantic analysis preserves known `time()`, `time_close()`, `timestamp()`, and `timeframe.*` return types for downstream diagnostics; binds `timestamp()` date-string, timezone-prefixed, and default-timezone numeric date overloads; preserves `timestamp(dateString)` as `const int` while calendar-argument overloads infer `simple` or `series int`; and diagnoses missing required `timestamp()` `year`/`month`/`day` fields, excess timestamp arguments, non-string time/session/timeframe helper arguments, non-numeric `bars_back` / `timeframe_bars_back`, invalid literal offset bounds, missing explicit timeframe arguments for `timeframe_bars_back`, plus non-numeric calendar/timeframe numeric arguments where overload binding is unambiguous. Exchange-calendar/session gap projection beyond deterministic chart-timeframe steps remains. |
| Built-ins | `color.*` | Partial | `tests/compat/pine-builtins.test.ts`; `tests/compat/pine-visuals.test.ts`; `src/semantic/checker.test.ts` | Runtime covers Pine v6 named constants, common constructors, channels, and gradients. Semantic analysis preserves known color constructor and channel return types and diagnoses non-color color arguments plus non-numeric channel, `transp`/`transparency`, and gradient threshold arguments for downstream diagnostics. Theme-sensitive behavior remains. |
| Built-ins | `str.*` | Partial | `tests/compat/pine-builtins.test.ts`; `src/semantic/checker.test.ts` | Runtime covers common format, search, split, case, trim, replace, and repeat helpers, including documented `str.repeat()` named arguments and `str.trim(na)` behavior. Semantic analysis preserves known string helper return types for downstream diagnostics, including `str.split()` array element types, and diagnoses non-string string parameters plus non-numeric time, position, count, and occurrence parameters. Formatting and Unicode edge cases remain. |
| Built-ins | `array.*` | Partial | `PINE_ARRAY_REFERENCE_INVENTORY.md`; `src/runtime/arrays.test.ts`; `tests/compat/pine-arrays.test.ts`; `tests/compat/pine-builtins.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; `src/semantic/checker.test.ts` | Runtime covers common constructors including `array.new<T>`, mutation, search, ordering, UDT `sort_field` sorting by field name or index, slicing, and statistical helpers. Public-script checkpoint coverage exercises persistent bounded signal arrays, queue shifting, copied sorted views, aggregate helpers, and table output. Semantic coverage preserves element types from constructors, homogeneous literals/`array.from`, array-returning helpers, element reads, scalar helper results, and typed collection loop symbols; it validates generic constructor template arity and qualifier misuse, reports conservative mutation/concat mismatches for known arrays, and enforces const int/string `array.sort()` `sort_field` arguments. Full official-reference reconciliation and edge cases remain. |
| Built-ins | `runtime.*` | Partial | `src/runtime/engine.test.ts`; `tests/compat/pine-language.test.ts`; `packages/tealchart/src/tealscript/TealscriptManager.test.ts`; `src/semantic/checker.test.ts` | Only `runtime.error()` is implemented; it halts execution with a stable `runtime.error` code in execution results and worker-facing error payloads, and semantic analysis reports non-string message arguments. |
| Built-ins | Global helpers | Partial | `src/runtime/engine.test.ts`; `tests/compat/pine-language.test.ts`; `src/semantic/checker.test.ts` | `na`, `nz`, `fixnan`, and primitive casts are covered for common numeric/source idioms; semantic analysis preserves known `nz()` and `fixnan()` return types for downstream diagnostics and diagnoses bool `na()` / `nz()` / `fixnan()` arguments before runtime. Broader type-system diagnostics and exact color overload parity are planned. |
| Built-ins | `map.*` | Partial | `src/runtime/maps.test.ts`; `tests/compat/pine-maps.test.ts`; `tests/compat/pine-language.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; `src/semantic/checker.test.ts`; Roadmap Epic 12 | Runtime covers constructors, size, put/get/contains/remove/clear/copy/keys/values/put_all, `map.put()` previous-value returns, named map reference arguments, map variable history references, value-key validation, insertion-order arrays, capacity checks, method syntax, generic constructor/declaration syntax, key-value loops, and map fields stored on UDTs. Public-script checkpoint coverage exercises persistent named signal maps, previous-value updates, copied snapshots, key-value iteration, missing-key checks, and table output. Semantic coverage reports invalid map constructor templates plus conservative key/value mismatches for known `map<K, V>` variables and unannotated `map.new<K, V>()` constructors, infers `map.get()` value types, and reports UDT map-field reference mismatches. Bare collection container names and parsed nested collection templates in template positions are rejected; Pine does not allow direct collection elements inside collections. Remaining gaps are complete reference-type enforcement. |
| Built-ins | `matrix.*` | Partial | `src/runtime/matrices.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; Roadmap Epic 12 | Runtime covers typed constructors, shape/access helpers, row/column mutation, copy/reshape, transpose/reverse, concat, matrix row iteration, full and ranged fills, sort/submatrix operations including named range arguments, UDT `sort_field` row sorting by field name or index, inspection predicates, basic aggregates, `matrix.sum()`/`matrix.diff()` for same-shape matrix and scalar operands, `matrix.mult()` for matrix/array/scalar operands, `matrix.pow()` for non-negative integer powers, `matrix.trace()`/`matrix.det()`/`matrix.inv()` for square matrices, `matrix.pinv()` for rectangular or rank-deficient matrices, `matrix.rank()` for rectangular or square matrices, `matrix.eigenvalues()` for real eigenvalues of square numeric matrices, `matrix.eigenvectors()` for real eigenvector columns, and `matrix.kron()` for Kronecker products. Public-script checkpoint coverage exercises matrix scoreboards that combine weighted factor matrices, row extraction, transpose reads, aggregate scores, and table output. Semantic coverage infers `matrix.new<T>()` constructor element types, reports invalid generic constructor templates, returns known `matrix.get()` element types, infers matrix row arrays for collection loops, reports conservative `matrix.set()`/`matrix.fill()` element mismatches, and enforces const int/string `matrix.sort()` `sort_field` arguments. Remaining gaps include exact repeated-eigenvalue basis parity. |
| Data | `request.*` | Partial | `tests/compat/pine-request-security.test.ts`; `tests/runtime/requestDatafeed.test.ts`; `src/semantic/checker.test.ts`; `FOOTPRINT_REQUEST_DESIGN.md`; Roadmap Epic 8 | `request.security()`, `request.security_lower_tf()`, `request.currency_rate()`, point-series families (`dividends`, `earnings`, `splits`, `financial`, `economic`), `request.seed()`, dynamic request guards, limits, and deterministic datafeed routing are covered. Semantic analysis preserves supported request return types for downstream diagnostics, including expression-preserving security/seed calls, lower-timeframe arrays, float point-data helpers, invalid literal `calc_bars_count`, barmerge mode, point-series field selector, non-boolean `ignore_invalid_*` option diagnostics, non-string request identifier diagnostics, and the explicit planned-unsupported `request.footprint()` diagnostic. Data availability remains host/provider-gated, point-series lookahead is conservative, and `request.footprint()` remains planned until host footprint/intrabar-volume data exists. |
| Data | `ticker.*` | Partial | `tests/compat/pine-ticker.test.ts`; `tests/compat/pine-unsupported.test.ts`; `src/semantic/checker.test.ts`; Roadmap Epic 9 | Common ticker constructors and non-standard chart request IDs are covered for indicator workflows. Semantic analysis preserves supported `ticker.*` helper return types for downstream diagnostics, reports invalid literal session/adjustment/back-adjustment/settlement-as-close modifier values, and reports planned unsupported constructors such as `ticker.rangebar()` explicitly. Synthetic strategy/backtest behavior remains planned for Epic 14. |

### Visual Outputs

| Area | Feature | Status | Evidence | Remaining gaps |
| --- | --- | --- | --- | --- |
| Visuals | `plot` | Partial | `tests/compat/pine-basics.test.ts`; `tests/compat/pine-visuals.test.ts`; `tests/runtime/runtime.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime captures common v6 style/display/format/precision/force-overlay/line-style/show-last/histbase/trackprice/join/explicit-z-order metadata plus legacy named `transp` color transparency, semantic analysis preserves `plot()` handle return types and diagnoses invalid literal style/linestyle, display, format/precision, linewidth values, non-string title values, non-color color values, non-boolean trackprice/join/editable/force-overlay values, and non-numeric linewidth/histbase/offset/show-last/precision/transp values for downstream diagnostics, and renderer coverage applies line styles, line-vs-linebr `na` bridging, force-overlay routing, common offsets, display-none hiding, show-last windows, histbase baselines, trackprice lines and value labels, explicit fill/plot/hline z-ordering, joined point markers, stepline diamond markers, and a common visual command-trace regression. Full renderer pixel parity remains. |
| Visuals | `hline` | Supported | `tests/compat/pine-visuals.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime captures common settings, semantic analysis preserves `hline()` handle return types and diagnoses invalid literal linestyle/linewidth/display values plus non-string title, non-color color values, non-boolean editable values, and non-numeric price/linewidth values for downstream diagnostics, and renderer coverage applies color, linewidth, line style, display hiding, and pane coordinates. None known for current output shape. |
| Visuals | `fill` | Supported | `tests/compat/pine-visuals.test.ts`; `tests/runtime/runtime.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime captures common fill metadata and legacy named `transp` color transparency, semantic analysis preserves plot/hline handle types and diagnoses invalid literal display values plus non-string title, non-color color values, non-boolean editable/fillgaps values, and non-numeric show-last/transp values before fill usage, and renderer coverage applies plot/hline handles, gaps, display hiding, color series, and show-last behavior. None known for current output shape. |
| Visuals | `bgcolor` | Supported | `tests/compat/pine-visuals.test.ts`; `tests/runtime/runtime.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime captures display, force-overlay metadata, and legacy named `transp` color transparency, semantic analysis diagnoses invalid literal display values, non-string title values, non-color color values, non-boolean editable/force-overlay values, and non-numeric offset/show-last/transp values, and renderer coverage applies display hiding plus main, indicator pane, and force-overlay coordinates. None known for current output shape. |
| Visuals | `plotshape`, `plotchar`, `plotarrow` | Partial | `tests/compat/pine-visuals.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime captures common style/location/display/format/precision/force-overlay metadata and normalized arrow colors. Semantic diagnostics cover invalid literal display values, format/precision values, marker style/location/size values, plotarrow min/max height values, non-string marker title/text/char values, non-color marker/arrow color values, non-boolean editable/force-overlay values, and non-numeric offset/show-last/precision plus plotarrow series/min/max height values. Renderer coverage applies marker text including newline-separated labels, offsets, display/show-last routing, force-overlay routing, indicator pane coordinates, plotarrow min/max height scaling, and Pine flag/label marker bodies. Full marker pixel parity remains. |
| Visuals | `barcolor` | Supported | `tests/compat/pine-visuals.test.ts`; `tests/runtime/runtime.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime applies legacy named `transp` color transparency. Semantic analysis diagnoses non-string title values, non-color color values, non-boolean editable values, and non-numeric offset/show-last/transp values. Renderer coverage applies display hiding and show-last windows to candle color overrides. None known for current output shape. |
| Visuals | `plotbar`, `plotcandle` | Supported | `tests/compat/pine-real-checkpoints.test.ts`; `tests/compat/pine-visuals.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime captures display, format, precision, force-overlay metadata, and a source-linked public Heikin-Ashi custom-candle overlay. Semantic diagnostics cover missing, unknown, duplicate OHLC output argument bindings, invalid literal display/format/precision values, non-string title values, non-color body/wick/border color values, non-boolean editable/force-overlay values, and non-numeric OHLC/show-last/precision values. Edge-case parity will continue under visual epic. |
| Drawings | `label.*` | Partial | `src/runtime/drawings/builtins.test.ts`; `tests/compat/pine-drawings.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts`; `packages/tealchart/src/rendering/TealScriptDrawingRenderer.test.ts` | Runtime covers coordinate and chart-point constructors, mutation, copy/delete, getters, text alignment metadata, font-family metadata, text formatting metadata, GC limits, and realtime rollback. Public-script checkpoint coverage exercises persistent last-bar signal labels with multiline text, dynamic style/color/textcolor, alignment, font-family, formatting, tooltip, getter plots, and `label.all` count output. Semantic analysis preserves known label getter return types and `label.all` handle-array element types and diagnoses invalid literal xloc/yloc/style/size/text alignment/font-family/text-formatting values plus non-string text/tooltip values, non-color color/textcolor values, non-numeric constructor/setter coordinate values, and non-boolean `force_overlay` values for downstream diagnostics. Renderer covers script-pane routing plus text-only, directional label, common symbol styles, multiline label text, label text alignment, default/monospace font-family metadata, and bold/italic text formatting. Remaining gaps are TradingView-exact pixel geometry and edge-case style parity. |
| Drawings | `line.*` | Partial | `tests/compat/pine-real-checkpoints.test.ts`; `src/runtime/drawings/builtins.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime covers `chart.point` overloads, point endpoint setters, GC limits, realtime rollback, source-linked public supply/demand-zone midline and channel linefill idioms, and a source-linked public trendline signal checkpoint that exercises endpoint, extension, color, style, width, `line.get_price()`, and `line.all` output behavior. Semantic analysis preserves known line getter return types and `line.all` handle-array element types and diagnoses invalid literal xloc/extend/style values plus non-color color values, non-numeric coordinate/width values, and non-boolean `force_overlay` values for downstream diagnostics. Renderer covers dashed/dotted and arrowhead line styles. Remaining gaps are full TradingView pixel geometry and edge-case parity. |
| Drawings | `linefill.*` | Partial | `tests/compat/pine-real-checkpoints.test.ts`; `src/runtime/drawings/builtins.test.ts`; `tests/compat/pine-drawings.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts` | Runtime covers fill lifecycle, `linefill.all`, handle validation, and a source-linked public channel fill over persistent upper/lower line handles. Semantic analysis preserves linefill getter line-handle returns and `linefill.all` handle-array element types and diagnoses non-color color values for downstream diagnostics. Full line coordinate/object parity remains. |
| Drawings | `box.*` | Partial | `tests/compat/pine-real-checkpoints.test.ts`; `src/runtime/drawings/builtins.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/TealchartRenderer.test.ts`; `packages/tealchart/src/rendering/TealScriptDrawingRenderer.test.ts` | Runtime covers constructor, mutation, copy/delete, getters, Pine default text-size metadata, text layout metadata, text formatting metadata, GC limits, common `chart.point` overloads, point corner setters, `box.set_xloc`, source-linked public supply/demand-zone box idioms, and a source-linked public box text-layout checkpoint that exercises extension, border, fill, multiline text, alignment, wrapping, font-family, formatting, getter plots, and `box.all` output. Semantic analysis preserves known box getter return types and `box.all` handle-array element types and diagnoses invalid literal xloc/extend/border-style/text alignment/text wrap/text-size/font-family/text-formatting values plus non-string text values, non-color border/background/text color values, non-numeric coordinate/border-width values, and non-boolean `force_overlay` values for downstream diagnostics. Renderer covers filled boxes, borders, alignment, explicit multiline text, `text_wrap=auto`, numeric text sizes, default/monospace font-family metadata, and bold/italic text formatting. Remaining gaps are TradingView-exact text pixels and edge-case styling. |
| Drawings | `polyline.*` | Partial | `tests/compat/pine-real-checkpoints.test.ts`; `src/runtime/drawings/builtins.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/rendering/TealScriptDrawingRenderer.test.ts` | Runtime covers constructor, copy/delete, limits, all-id arrays, transient same-bar realtime rollback, and a source-linked public zigzag path over `chart.point` vertices. Semantic analysis preserves constructor/copy handle return types and `polyline.all` handle-array element types and diagnoses invalid literal xloc/line-style values plus non-color line/fill color values, non-numeric line-width values, and non-boolean `curved`/`closed`/`force_overlay` values for downstream diagnostics. Renderer covers fixed path geometry, approximate curved paths, optional fill, and line styling. Remaining gaps are full Pine lifecycle parity and TradingView-exact curved geometry. |
| Drawings | `table.*` | Partial | `src/runtime/drawings/builtins.test.ts`; `tests/compat/pine-drawings.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; `src/semantic/checker.test.ts`; `packages/tealchart/src/rendering/TealScriptDrawingRenderer.test.ts` | Runtime covers fixed-position handles, delete/clear, `table.merge_cells`, `table.all`, cells, table-level position/background/frame/border setters, cell text/color/background/size setters, width/height setters, alignment setters, font-family metadata, text formatting metadata, tooltip metadata, persistent table realtime rollback, and a 10,000-cell sandbox cap. Source-linked public dashboard-table setter coverage locks last-bar table position/frame/border updates, merged header spans, cell dimensions, alignment, colors, and tooltip payloads. Semantic analysis preserves constructor handle return types and `table.all` handle-array element types and diagnoses invalid literal table position and cell text alignment/text-size/font-family/text-formatting values plus non-string cell text/tooltip values, non-color table/cell color values, and non-numeric table dimension, coordinate, width, height, frame, and border values for downstream diagnostics. Renderer covers fixed table placement, measured automatic cell sizes, multiline cell text, percentage-based explicit cell sizes, merged cell spans, backgrounds, borders, frames, text alignment, numeric text sizes, default/monospace font-family metadata, and bold/italic text formatting. Remaining gaps are TradingView-exact sizing and pixel parity. |

### Alerts, Strategies, And Data

| Area | Feature | Status | Evidence | Remaining gaps |
| --- | --- | --- | --- | --- |
| Alerts | `alertcondition` / `alert` | Supported | `tests/compat/pine-control-time.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts`; Roadmap Epic 13 | TealScript emits runtime/worker alert outputs with placeholders, frequencies, strategy fill messages, realtime markers, semantic call-shape diagnostics, invalid literal `alert()` frequency diagnostics, non-string alert message/title/frequency diagnostics, and non-boolean `alertcondition()` condition diagnostics. Host-product UI presentation is outside this package. |
| Runtime | Pine Logs `log.*` | Supported | `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts`; Roadmap Epic 13 | TealScript emits runtime/worker log outputs with levels and formatted messages, and semantic analysis reports non-string message arguments. Host-product UI presentation is outside this package. |
| Strategies | `strategy.*` | Supported | `src/parser/parser.test.ts`; `src/runtime/engine.test.ts`; `src/semantic/checker.test.ts`; `tests/compat/pine-real-checkpoints.test.ts`; Roadmap Epic 14 | TealScript implements a deterministic OHLC broker emulator with settings, orders, fills, open/closed trades, position/equity state, per-bar equity curve snapshots, commissions, runup/drawdown, trade accessors, public stats-table output over strategy counters, public trade-list table output over closed-trade accessors, public open-position dashboard output over open-trade accessors, `strategy.exit()` profit/loss tick-distance brackets, trailing exit tick-distance activation/offsets, order-fill alerts, `strategy.risk.max_position_size()` entry exposure caps, same-UTC-day non-exit `strategy.risk.max_intraday_filled_orders()` caps, UTC closed-trade `strategy.risk.max_cons_loss_days()` caps, same-UTC-day equity-curve `strategy.risk.max_intraday_loss()` caps, overall equity-curve `strategy.risk.max_drawdown()` caps, market risk-exit orders for open positions after shared risk stops, semantic call-shape plus return-type diagnostics, literal strategy declaration value diagnostics, non-boolean, non-numeric, and non-string strategy declaration diagnostics, non-boolean order alert/immediate flag diagnostics, non-string order id/comment/alert diagnostics, non-string order/risk enum-like diagnostics, non-numeric order/risk sizing and trade accessor index diagnostics, literal order id/direction/OCA/quantity diagnostics, and excess positional order argument diagnostics. Full TradingView intrabar path parity, bar magnifier, lower-timeframe fill simulation, and exact strategy risk session halt behavior remain deferred until TealScript has an explicit intrabar/session risk model. |
| Data | Multi-timeframe requests | Supported | `tests/compat/pine-request-security.test.ts`; Roadmap Epic 8 | Deterministic higher-timeframe `request.security()` gap/lookahead behavior is covered; live host data availability is outside TealScript runtime scope. |
| Data | Other-symbol requests | Supported | `tests/compat/pine-request-security.test.ts`; `tests/runtime/requestDatafeed.test.ts`; Roadmap Epic 8 | Other-symbol metadata/currency routing is covered through host-provided request contexts. |

## Parity PR Checklist

Every feature-parity PR should include the relevant items below. If an item is
not relevant, call that out in the PR description.

- Copy-paste parse/execute checkpoint when the change targets a public Pine
  idiom.
- Failure classification update when a corpus or checkpoint failure changes
  status.
- Parser fixture for new syntax or grammar shape.
- Runtime fixture over deterministic local bars for new execution behavior.
- Reduced Pine idiom checkpoint for public or documented Pine patterns.
- Negative diagnostic fixture for unsupported or invalid forms.
- Compatibility matrix update.
- Roadmap update when the phase completes or a gap changes status.

## Checkpoint Fixture Rules

Checkpoint fixtures should be deterministic and small:

- Use local bar arrays from the existing fixture helpers.
- Assert concrete plot, drawing, alert, or error outputs.
- Preserve semantic shape from official docs or common public Pine idioms.
- Avoid depending on TradingView, network access, current market data, or random
  online scripts at test time.
