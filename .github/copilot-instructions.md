# Copilot Instructions

This is a Tealstreet mirror repository. Read the root `AGENTS.md` before making
changes, then read the nearest package `CLAUDE.md` for package-specific context.

Use feature branches from the latest `master`, run `just check` before opening a
PR, and do not add release or publish automation.
